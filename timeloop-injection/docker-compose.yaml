version: '3'

services:
  labs:
    hostname: labs
    image: jaylenwang/timeloop-injection:latest
    # image: fb5752be38c6
    ports:
      - "8888:8888"
    volumes:
      - ./workspace:/home/workspace
    environment:
      - USER_UID=501
      - USER_GID=501